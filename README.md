# Generate TLS secrets and Mosquitto configuration
This small script conveniently generates the secrets in x509 protocol ready to use in e.g. MQTT. 

Also generates ready configuration for `mosquitto` broker.

# Copyright 